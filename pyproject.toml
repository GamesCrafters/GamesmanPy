[project]
name = "gamesmanpy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.0",
]

[workspace]
members = ["database", "solver", "models", "server", "interfaces", "games"]